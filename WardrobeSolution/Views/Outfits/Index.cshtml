@model IEnumerable<WardrobeSolution.Models.Outfit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OutfitName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccessoryID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BottomID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TopID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OutfitName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccessoryID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BottomID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TopID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OutfitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OutfitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OutfitID })
        </td>
    </tr>
}

</table>
